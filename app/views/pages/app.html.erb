<!-- <h1>Pages#app</h1>
<p>React app is loaded here from <strong>app/javascript/packs/application.js</strong>:</p> -->
<div class="header">
        <h1>MUVEE</h1>
        <% if @current_user.present? %>
        <div class="profileImage">
          Welcome, <%= @current_user.name %>
          <%= cl_image_tag @current_user.profile_image, :width=>100, :height=>100, :gravity=>"faces", :crop=>"fill" %>
          <% end %>
        </div>
    <!-- ///////////Nav Items///////////// -->
</div>
<div class="navbar">

             <%= link_to 'Home', root_path %>

             <% if @current_user.present? %>
              <%= link_to 'My Account', edit_user_path(@current_user.id) %>
             <%= link_to 'Logout', login_path, method: 'delete' %>
             <% else %>

            <div>
             <%= link_to 'Login', login_path %>
             </div>

             <%= link_to 'Sign up', new_user_path  %>
             <% end %>
</div>

<div id="app"></div>


<%= javascript_pack_tag 'application' %>


<div class="footer">
  <h2>Footer</h2>
</div>
